# Used by docker-compose.yml to pass the SQL Server login password into the
# backend container without hardcoding it in a committed file.
# Copy this to ".env" (same folder as docker-compose.yml) and fill in the
# password you set for the cqnms_app SQL login - see database-docker-setup.md.
DB_PASSWORD=
